({
  /*  showErrorMessage: function (component) {
        var toastEvent = $A.get("e.force:showToast");
        toastEvent.setParams({
            title: "Error",
            message: "An iCoach Form Objective can only be worked on from an iCoach Form",
            type: "error"
        });
        toastEvent.fire();

        // Prevent the page from navigating by closing the quick action
        $A.get("e.force:closeQuickAction").fire();
    }*/
})